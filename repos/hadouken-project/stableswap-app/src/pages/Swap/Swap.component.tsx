import React from 'react'

import SwapModule from '@modules/SwapModule'
import { Box } from '@mui/material'

const Swap: React.FC = () => {
  return (
    <Box marginBottom={5}>
      <SwapModule />
    </Box>
  )
}

export default Swap
