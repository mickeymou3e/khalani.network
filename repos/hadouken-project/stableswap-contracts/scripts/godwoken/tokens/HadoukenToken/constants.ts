import path from 'path'

import { TOKENS_CONTRACTS_DIR } from '../constants'

export const CONTRACT_NAME = 'HadoukenToken'

export const HADOUKEN_TOKEN_CONTRACTS_DIR = path.join(
  TOKENS_CONTRACTS_DIR, 
  CONTRACT_NAME,
)