const SUB_LABEL = 'Select token with amount'

const BUTTON_NAME = 'Redeem'

const ORIGIN_TITLE = 'Token'

const SETTINGS_LABEL = 'Settings'
const CLOSE_LABEL = 'Close'

const CHAIN_NAME = 'Ethereum'

export const messages = {
  SUB_LABEL,
  BUTTON_NAME,
  ORIGIN_TITLE,
  SETTINGS_LABEL,
  CLOSE_LABEL,
  CHAIN_NAME,
}
