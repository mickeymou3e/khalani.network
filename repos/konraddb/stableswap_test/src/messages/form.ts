const SLIPPAGE_SELECTOR_TITLE = 'Slippage'
const SLIPPAGE_SELECTOR_DESCRIPTION = `Selecting slippage option will present minimal values user will get in
 target tokens.`

const MAXIMUM_SLIPPAGE = 'The maximum slippage for the operation is'
const USE_MAX = 'Use max'
const AVAILABLE = 'Available'

const BALANCE = 'Balance'

export const messages = {
  SLIPPAGE_SELECTOR_TITLE,
  SLIPPAGE_SELECTOR_DESCRIPTION,
  MAXIMUM_SLIPPAGE,
  AVAILABLE,
  USE_MAX,
  BALANCE,
}
