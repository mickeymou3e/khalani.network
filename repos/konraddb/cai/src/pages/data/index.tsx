import { AppLayout } from "@/layout/AppLayout";
import { DataModule } from "@/modules";

const Data = () => {
  return (
    <AppLayout>
      <DataModule />
    </AppLayout>
  );
};

export default Data;
