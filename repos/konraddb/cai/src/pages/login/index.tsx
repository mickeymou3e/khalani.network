import { AppLayout } from "@/layout/AppLayout";
import { LoginModule } from "@/modules";

const Login = () => {
  return (
    <AppLayout>
      <LoginModule />
    </AppLayout>
  );
};

export default Login;
