import { AppLayout } from "@/layout/AppLayout";
import { MeasureModule } from "@/modules";

const Data = () => {
  return (
    <AppLayout>
      <MeasureModule />
    </AppLayout>
  );
};

export default Data;
