export enum PoolType {
  Unknown = 'Unknown',
  Stable = 'Stable',
  Weighted = 'Weighted',
  Linear = 'Linear',
  AaveLinear = 'AaveLinear',
  StablePhantom = 'StablePhantom',
  ComposableStable = 'ComposableStable',
}

export enum PoolOwnType {
  CCLP,
  CALP,
}
