import Task, { TaskMode } from '../../../../src/task';

const ComposableStablePoolTask = new Task('20220906-composable-stable-pool', TaskMode.READ_ONLY);

export default {
  ComposableStablePoolTask,
};
