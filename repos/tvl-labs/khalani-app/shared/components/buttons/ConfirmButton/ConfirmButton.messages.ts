const CHANGE_NETWORK_LABEL = 'Change network'
const INSUFFICIENT_BALANCE_LABEL = 'Insufficient balance'

const CONNECT_WALLET_LABEL = 'Connect wallet'

export const messages = {
  CHANGE_NETWORK_LABEL,
  INSUFFICIENT_BALANCE_LABEL,
  CONNECT_WALLET_LABEL,
}
